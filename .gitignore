.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
encrypted-ixs/arcis_temp_target
build
encrypted-ixs/src/main.rs
artifacts/

# Submission prep — local-only, has its own private git repo
submission/

# Local-only environment (Helius API keys, RPC URLs, etc.)
.env
.env.local
.env.*.local

# Next.js build output
.next/
