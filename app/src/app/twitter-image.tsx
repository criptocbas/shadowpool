// Twitter card reuses the OG image; Next picks up this file
// automatically for the `twitter:image` meta. Same design, same
// dimensions — social previews stay consistent across surfaces.
export { default } from "./opengraph-image";
export { alt, size, contentType } from "./opengraph-image";
